'use client'
import usePokemon from "@/hooks/usePokemon";

const PokemonList = () => {
  const { } = usePokemon()
  return (
    <div>
      <h1>Pokemon List</h1>
    </div>
  );
};

export default PokemonList;
